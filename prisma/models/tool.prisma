model Tool {
    id          Int       @id @default(autoincrement())
    name        String    @db.VarChar(255)
    sn          String   @unique @db.VarChar(255)
    controlDate DateTime? @db.Date
    user        User?     @relation(fields: [userId], references: [id])
    userId      Int?
}
